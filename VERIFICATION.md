# 🔍 Verification Guide | دليل التحقق

**English:**  
This document provides evidence that all images are generated automatically without manual editing.

**العربية:**  
هذا المستند يقدم دليلاً على أن جميع الصور تم توليدها تلقائياً دون أي تعديل يدوي.

---

## 📸 Terminal Execution Screenshot | لقطة شاشة تنفيذ الطرفية

*[Placeholder: Add a screenshot of running `python code/quran_hearts.py` in terminal]*

**English - How to capture:**
1. Open terminal in project root
2. Run: `cd code && python quran_hearts.py`
3. Take screenshot showing the generation process

**العربية - كيفية التقاط اللقطة:**
1. افتح الطرفية في المجلد الرئيسي للمشروع
2. نفّذ: `cd code && python quran_hearts.py`
3. التقط لقطة شاشة تظهر عملية التوليد

---

## 🖼️ Visual Comparison: Ar-Rahman vs ❤️ Emoji | المقارنة البصرية: الرحمن مقابل ❤️

*[Placeholder: Add side-by-side comparison image]*

**Image 1:** `055_Ar-Rahman.png` (generated bitmap | الصورة المولدة)  
**Image 2:** ❤️ Emoji

**English:**  
This comparison demonstrates that the heart-like pattern in Ar-Rahman Surah is not a visual illusion but a reproducible algorithmic result.

**العربية:**  
هذه المقارنة تثبت أن النمط الشبيه بالقلب في سورة الرحمن ليس وهمًا بصريًا بل نتيجة خوارزمية قابلة للتكرار.

---

## 🚀 Run Online (Google Colab) | التشغيل أونلاين

**English:**  
You can run this experiment directly in your browser using Google Colab:

1. Upload `notebook.ipynb` to Google Colab
2. Upload the `code/` directory
3. Click **Run All**

**العربية:**  
يمكنك تشغيل هذه التجربة مباشرة في المتصفح باستخدام Google Colab:

1. ارفع `notebook.ipynb` إلى Google Colab
2. ارفع مجلد `code/`
3. اضغط **Run All**

**Or use this direct link (if you create a Colab notebook):**  
*[Add Colab link here if available]*

---

## ✅ Verification Checklist | قائمة التحقق

- [x] All 114 images generated automatically | تم توليد جميع الصور الـ 114 تلقائياً
- [x] No manual image editing | لا يوجد تعديل يدوي على الصور
- [x] Reproducible results (same input = same output) | نتائج قابلة للتكرار (نفس المدخل = نفس المخرج)
- [x] Code is open source and verifiable | الكود مفتوح المصدر وقابل للتحقق
- [x] Methodology is documented | المنهجية موثقة

---

## 🔬 How to Verify Yourself | كيفية التحقق بنفسك

**English:**

1. **Clone the repository:**
   ```bash
   git clone https://github.com/Alaaharoun/Rahman-Key.git
   cd Rahman-Key
   ```

2. **Install dependencies:**
   ```bash
   pip install -r requirements.txt
   ```

3. **Delete existing images (optional, to test from scratch):**
   ```bash
   rm images/*.png
   rm descriptions.json
   ```

4. **Run the code:**
   ```bash
   cd code
   python quran_hearts.py
   ```

5. **Verify:**
   - Check that 114 PNG files are created in `images/`
   - Check that `descriptions.json` is created
   - Compare your results with the repository images

**العربية:**

1. **استنسخ المستودع:**
   ```bash
   git clone https://github.com/Alaaharoun/Rahman-Key.git
   cd Rahman-Key
   ```

2. **ثبّت المتطلبات:**
   ```bash
   pip install -r requirements.txt
   ```

3. **احذف الصور الموجودة (اختياري، للاختبار من الصفر):**
   ```bash
   rm images/*.png
   rm descriptions.json
   ```

4. **شغّل الكود:**
   ```bash
   cd code
   python quran_hearts.py
   ```

5. **تحقق:**
   - تأكد من إنشاء 114 ملف PNG في `images/`
   - تأكد من إنشاء `descriptions.json`
   - قارن نتائجك مع صور المستودع

---

## 📝 Notes | ملاحظات

**English:**
- All images are generated using the exact same algorithm
- The methodology is fixed: 31×6 bits for all Surahs
- No cherry-picking or manual selection of results
- Every Surah follows the same transformation process

**العربية:**
- جميع الصور تم توليدها باستخدام نفس الخوارزمية بالضبط
- المنهجية ثابتة: 31×6 بت لجميع السور
- لا يوجد انتقاء يدوي أو اختيار للنتائج
- كل سورة تتبع نفس عملية التحويل

---

**🌙 Rahman-Key** — Transparent, reproducible, verifiable. | شفاف، قابل للتكرار، قابل للتحقق.

